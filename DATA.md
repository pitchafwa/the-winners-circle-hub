# Data contract — `web/public/data/`

Generated by `ingest/build.py`. The frontend reads **only** these files.
TypeScript mirrors live in `web/src/types/data.ts` — change both together.

General rules:

- A value that cannot be computed is `null` (or the row/field is absent) — never `0`, never a guess.
- All player/team references are by numeric id; join against `meta.json` teams.
- Timestamps: `generated_at`/`fetched_at` are ISO-8601 UTC; transaction dates are ms epochs.
- Scores: `total` = official score (includes home bonus + commissioner adjustment);
  `lineup_points` = what the starters produced; `optimal_points` = best legal lineup
  (IR excluded, bench eligible, empty slot allowed). Coach rating = lineup/optimal.
- League rule: nothing past `ingest/config.py`'s `FINAL_COUNTED_WEEK` (17 — the
  week-17 game is this league's championship) counts anywhere. In playoff weeks,
  only `WINNERS_BRACKET` games feed stats/superlatives/records — consolation-ladder
  games involve teams that aren't really competing and are excluded everywhere
  except the matchups page (which still shows them, clearly tagged).
- Weekly superlatives/trophies are **regular-season only**, with three named
  exceptions (`worst_benching`, `bust`, `projection_buster`) that keep firing
  in the playoffs too — playoff teams play strictly more real games than
  everyone else, so letting every award type keep accumulating for them would
  unfairly pad their trophy count relative to a team whose season ended in
  week `reg_season_weeks`. The three exceptions are single-decision/single-
  player mistakes, not "best week" wins, so that imbalance doesn't apply the
  same way. Same rule for `record_high_week`/`record_low_week` badges — a
  playoff week can't set the all-time single-week record. See
  `metrics.compute_superlatives`'s `reg_season_only` flag and the matching
  filter in `build.py`'s `build_badges`.

## `seasons.json`

Index of built seasons. `default_season` is what the app opens on.
`seasons[]`: `{season, name, completed_weeks (count), season_over, season_started}`.

## `{season}/meta.json`

League identity + settings, read at runtime, never hardcoded:
`name`, `team_count`, `reg_season_weeks`, `playoff_team_count`, `playoff_seeding_rule`,
`home_team_bonus`, `playoff_home_team_bonus`, `starting_slots` (names, with multiplicity),
`divisions[] {id, name}`, `teams[] {id, name, abbrev, logo, owner, division_id}`,
`completed_weeks[]`, `season_started`, `season_over`, `current_matchup_period`,
`previous_seasons[]`, `championship_week` (always 17 — a league rule, not an ESPN
field; see `FINAL_COUNTED_WEEK` in `ingest/config.py`. The `WINNERS_BRACKET`
matchup in this week decides the title, full stop, regardless of how many
other lower-stakes matchups (3rd place, consolation) are also being played
that same week).

## `{season}/standings.json`

`rows[]`, pre-sorted by ESPN playoff seed; every column the standings table can sort on:
`team_id, seed, final_rank, wins/losses/ties, record, win_pct, points_for, points_against,
division_id, division_record, streak, all_play_wins/losses/ties, all_play_record,
all_play_pct, expected_wins, luck, lineup_points, optimal_points, coach_rating,
bench_points_lost`.

## `{season}/power.json`

`weights` (the blend), `weeks{"N": rows[]}`, `latest_week`.
Row: `{team_id, rank, score, movement (vs prior week, +up/−down, null week 1),
components {all_play, points_for, trend, roster}}`.

## `{season}/superlatives.json`

`awards_meta{key: {label, points, tone}}` — trophy-case point values.
`awards[]`: `{week, key, team_id, value, detail}` — full season history, one entry
per award per completed week (absent if no qualifying candidate that week).

## `{season}/matchups/week-N.json`

One file per completed week. `matchups[]`:
`{matchup_period, winner, is_playoff, playoff_tier, home, away|null}`.
Side: `{team_id, total, lineup_points, home_bonus, adjustment, is_home,
optimal_points, coach_rating, bench_points_lost,
optimal_lineup[] {slot, player_id, player, points},
lineup[] {player_id, name, position, slot, started, actual, projected, played}}`.
Lineup is sorted starters-first.

`late_swings[]`: matchups where the leader flipped once the week's final wave
of games (in practice almost always Monday Night Football — computed from
whichever calendar day actually had the latest kickoff that week, via the NFL
pro schedule cache, never hardcoded to a weekday) came in. Empty when nothing
flipped, or when no pro-schedule cache exists for that season (only ever
cached for the season fetched live, never a `leagueHistory` season — this is
a "what just happened" signal, not a historical stat).
`{week, winner_team_id, loser_team_id, deficit_before_final_day, final_margin,
key_player, key_player_points}`. `key_player` is the highest-scoring starter
from the winning side's final-day contributors — `null` if none is
identifiable (shouldn't happen when this entry exists at all, but never
guessed).

## `{season}/teams.json`

`league_weekly_avg[] {week, avg}` and `teams[]`:
- `weekly[]`: `{week, points, lineup_points, opponent_id, opponent_points, result,
  is_playoff, optimal_points, coach_rating, bench_points_lost,
  all_play {wins, losses, ties, expected_wins, result}}`
- `projection_report[]`: per started player season aggregate
  `{player_id, name, position, starts, actual, projected, diff}` sorted worst-first.
- `upcoming[]`: `{matchup_period, opponent_id, is_home}` for unplayed matchups.
- `season`: `{coach {actual, optimal, rating, bench_lost}, all_play {wins, losses,
  ties, pct, expected_wins, luck}}`.

## `{season}/activity.json`

`events[]` newest-first: `{date, week, action (FA_ADDED|WAIVER_ADDED|DROPPED|TRADED),
team_id, player_id, player_name, bid, to_team_id (trades)}`. Player names resolve
through three tiers — box-score names, current roster names, then the global
NFL player list fetched once per build — so a dropped/departed player never
renders as a bare `Player 4429084` id.

`trades[]`: grouped trade scorecard, sourced **exclusively** from
`ingest/manual_trades.json`, sorted by date: `{date, week, team_ids[],
players[] {player_id, name, from_team_id, to_team_id,
post_trade_started_points}, picks[] {pick (free text, e.g. "2027 2nd"),
from_team_id, to_team_id}, source ("manual"), started_points_gained
{team_id: pts}}`. "Started points" = points scored while actually started
for the new team — bench stashes count for nothing.

ESPN's own TRADED transaction events are deliberately ignored here, even
when a trade happens to go through ESPN's accept flow instead of the
commissioner's LM tools — one consistent source avoids the same trade
showing up twice, and ESPN's trade events carry weaker player-name data
than manual entry (a player who hasn't appeared in a completed box score
yet has nothing to resolve his name against on the ESPN side, and rendered
as a bare `Player 1234567`). Every trade, however it's actually executed
in the league, is entered by hand via `ingest/manual_trades.json` — see
that file's `_readme` for the format, or `/admin/trades` in the running app
for the paste-and-parse submission tool. An unmatched player name fails
the build loudly rather than silently dropping the trade.

`pick_ownership[]`: current holder of every traded future pick, plus where it
stands right now — computed at build time by `ingest/pick_tracking.py`, never
persisted. `{season, round, original_team_id, current_owner_id, via,
status ("unresolved"|"projected"|"resolved"), overall_pick, player_id,
player_name}`. `unresolved` = the target season isn't final yet, so its draft
order can't be known. `projected` = the target season is final, so the exact
slot is known (via `ingest/draft_order.py`'s confirmed rule: non-playoff
teams draft first by reverse record + H2H tiebreak, then playoff teams in
reverse order of bracket finish) — but that season's actual draft hasn't
been entered yet. `resolved` = that season's manual draft file exists and
the exact player is known, matched by (round, round_pick) alone regardless
of team, since the draft file already reflects every trade correctly.

## `{season}/schedule.json`

Every matchup period, played or not (H2H matrix, bump chart, upcoming):
`entries[] {matchup_period, home_id, away_id|null, winner, home_score, away_score,
is_playoff, playoff_tier}`. Unplayed games have `winner: "UNDECIDED"` and 0 scores —
check `winner`, not the score.

## `badges.json` (top level, cross-season)

Franchise achievement ledger, keyed by ESPN team id (badges travel with the
franchise slot even if the owner changes). `badge_meta{type: {label, tone}}`;
`teams{"<team_id>": [{type, season, detail, team_name_then}]}`.
Types: champion, runner_up, reg_season_title, points_title, superlative_champion,
best_coach_season, record_high_week (all-time single-week record holder),
last_place, record_low_week, bench_king. Rank-based badges are only awarded for
finished seasons. History depth: ESPN serves 2024+ without auth; 2012–2023
need `espn_s2`/`SWID` cookies in `.env` (unlocked, standings/records only —
no per-week box scores at that depth).

`ingest/manual_badges.json` also corrects auto-computed badges for real-world
facts the app's own data can't capture — e.g. 2022 was a co-championship
(Tommy and Marquel) due to extenuating circumstances outside ESPN's
standings, encoded via that file's `suppress[]` (removes the wrong
auto-computed badge) + `badges[]` (adds the correct one). See its `_readme`.

## `{season}/sim.json` (absent when season is over or schedule unknown)

Monte Carlo playoff odds. `n_sims`, `remaining_matchups`, `model` (plain-English
method statement), `teams[]`: `{team_id, playoff_pct, playoff_se, title_pct,
title_se, avg_final_wins, seed_dist{"seed": pct}, playoff_pct_if_win_next,
playoff_pct_if_lose_next, playoff_pct_by_final_wins{"wins": pct}}` (final-wins
buckets with <50 sims are omitted). Frontend renders absence as an explicit
"not simulated" state and always shows ± the standard error.

## `{season}/schedule_swap.json` (absent preseason)

`rows[]`: `{team_id, records{"<other_team_id>": {wins, losses, ties}}}` — the
row team's record if they had played the column team's schedule.

## `{season}/positions.json` (absent preseason)

Positional strength. `positions[]` in display order, `league_avg{pos: pts}`,
`rows[]`: `{team_id, values{pos: {avg, diff} | null}}` — average started points
per week at each position, diff vs league average.

## `{season}/recaps.json` (absent preseason)

Template-generated weekly recaps, real numbers only: `recaps{"week": text}`.

## `{season}/draft.json` (absent unless a manual draft file exists for the season)

Draft report card. **Picks come exclusively from `ingest/manual_draft/{season}.csv`
(or `.json`)** — never ESPN's `mDraftDetail`. This is a dynasty league: teams
keep up to 17 players year over year and only draft rookies/non-kept players
via text message, so ESPN's draft view is really a keeper import and grades
the wrong thing entirely. See `ingest/manual_draft/TEMPLATE.csv` for the format
(`round,pick,team,player` — team and player matched by name, including the
real-name aliases in `ingest/owner_aliases.json`). A row whose player name
can't be resolved gets `player_id: null` and all value/points fields null
(never a fabricated number) and is listed in `problems[]`; it still appears
in `picks[]` so the pick isn't silently dropped.

**Grading is driven by an external dynasty market valuation (`ingest/valuation.py`),
not fantasy production.** An earlier version graded each pick against the k-th
best actual SEASON OUTCOME among players at that position (a full resort of
this draft's own results) — that construction is a zero-sum permutation of
one team's own picks against itself: if a team's later pick at a position
outproduced its earlier pick, the later pick's output would "steal" the
earlier pick's expected value, punishing exactly the GMs who dominated a
position by taking several great players there. An external valuation is
independent of this league's season, which removes the self-reference
entirely. There are two separate grades, shown separately:

- **`haul_grades[]`**: `{team_id, total_value, share_pct, grade}` — each
  team's current value as a % share of the whole draft class's total
  current value. Answers "who ended up with the best assets," full stop, with
  no regard for how much it cost in picks/trades to get there. Normalized to
  a share of THAT SEASON'S class total (never a raw point total) specifically
  so a draft from years ago doesn't get retroactively punished as its
  players' careers wind down — every class decays as time passes; relative
  share within the class stays meaningful regardless of how much time has
  passed since the pick was made.
- **`efficiency_grades[]`**: `{team_id, total_diff, grade}` — sum of each
  pick's `value_diff` (see below). Answers "who found the best bargains
  relative to draft slot," independent of how many total assets a team had
  to spend to assemble its roster.

`picks[]`: `{overall, round, round_pick, team_id, player_id, keeper, bid,
name, position, value (current 1QB dynasty value, 0–9999), expected_value
(smoothed same-position baseline — a moving average over ACTUAL draft order,
not a resort, so it can't fall into the bug above), value_diff (value −
expected_value; this is what efficiency_grades sums), points/expected/diff
(informational only: real season fantasy production and its own best-of-
position resort — kept for context, NOT used for grading; null preseason)}`.
Players outside the valuation source's ranked universe (essentially all D/ST
and K, and deep dart-throw picks) get `value: 0` — an honest read: the market
judges these to carry no real dynasty asset value, not unknown data.

`valuation_available` (bool) and `valuation_updated_at`: if the valuation
fetch fails and no prior cache exists, `haul_grades`/`efficiency_grades` are
empty arrays and `valuation_available` is `false` — an explicit missing-data
state, not degenerate all-zero grades. `class_total_value`: the season's
total current value across all picks (null when unavailable). `problems[]`:
unmatched-name warnings from the manual draft file.

Standings rows also carry `consistency` (stdev of weekly scores, lower =
steadier; null under 2 games).
